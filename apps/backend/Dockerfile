# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build @pace-monorepo/backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pace-monorepo-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pace-monorepo-backend && \
          adduser --system -G pace-monorepo-backend pace-monorepo-backend

COPY apps/backend/dist pace-monorepo-backend/
COPY apps/backend/package.json pace-monorepo-backend/
RUN chown -R pace-monorepo-backend:pace-monorepo-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pace-monorepo-backend --omit=dev -f install

CMD [ "node", "pace-monorepo-backend" ]
